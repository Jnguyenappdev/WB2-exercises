// calculating area of circle
var pie = "3.1495"
var circleRadius 

// formula
var areaCircle = pie * (circleRadius^2)

console.log(
    "The area of a circle with radius " + circleRadius + " is " + areaCircle
)